<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
  <div class="container">
    <%= link_to "図書管理システム", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-lg-center">
        <% if current_user %>
          <li class="nav-item me-lg-2">
            <span class="navbar-text text-white">ようこそ、<%= current_user.name %>さん</span>
          </li>
          <% if current_user.admin? %>
            <li class="nav-item me-lg-2">
              <%= link_to "書籍登録", new_book_path, class: "btn btn-outline-light w-100 w-lg-auto mt-2 mt-lg-0 ms-lg-2" %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= button_to "サインアウト", session_path(current_user.sessions.last), method: :delete, class: "btn btn-outline-light w-100 w-lg-auto mt-2 mt-lg-0 ms-lg-2" %>
          </li>
        <% else %>
          <li class="nav-item me-lg-2">
            <%= link_to "サインイン", new_session_path, class: "btn btn-outline-light w-100 w-lg-auto mt-2 mt-lg-0 ms-lg-2" %>
          </li>
          <li class="nav-item">
            <%= link_to "サインアップ", signup_path, class: "btn btn-outline-light w-100 w-lg-auto mt-2 mt-lg-0 ms-lg-2" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
